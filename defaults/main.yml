---
# defaults file for ansible-booked-apache

# using these defaults assumes that the role is deployed in a Docker container
# - DOCKER_HOST should be set as an environment variable and edited 
#   in a startup script 
# - the SSL certificate and key files should be mounted as volumes 
server_name: DOCKER_HOST
ssl_certificate_file: /etc/pki/tls/certs/docker-host-certificate.pem
ssl_certificate_key_file: /etc/pki/tls/private/docker-host.key

